## How run development environment?

Go to the main directory of your application and run the command:

```bash
docker-compose -f docker-compose-only-db.yml up --build
```

After this run command `yarn start:dev`
